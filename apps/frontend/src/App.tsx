import './App.css'
import Router from './Router';

const App = () => {
    return (
        <Router /> // This is the main entry point for the application, rendering the Router component
    )
};
export default App;
